{
  "name": "@tuitionflow/xrpl",
  "version": "0.1.0",
  "private": true,
  "description": "@tuitionflow/xrpl — reusable XRPL gateway (credentials, hash attestations, batch). No PII ever on-chain.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "jest",
    "test:integration": "jest -c jest.integration.config.js",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "xrpl": "^4.0.0"
  }
}
